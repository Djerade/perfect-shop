import theme from '@/styles/theme';
import { Text } from '@chakra-ui/react'
const Navebar = () => {
    return (
        <div>
            <Text color={theme.colors.text} variant=''>nave bare</Text>
        </div>
    )
}
export default Navebar;